  <div class="row">
   <%= form_tag @saml_login_url, :method => :post, :id => "saml-form" do  %>
     <%= params.each do |key, value|  %>
	     <% if (key == 'SAMLRequest') %>
		     <%= hidden_field_tag key, value  %>
	     <% end %>
    <% end %>
    <button class="column button button--nomargin secondary light" type="submit" name="authorize">Post</button>
  <% end %>

  <div class="row">
	  <form action="https://sso.comune.milano.it:443/openam/SSORedirect/metaAlias/Servizi/IDP_CdM" method="get">
            <%= params.each do |key, value|  %>
             <% if (key == 'SAMLRequest') %>
                <%= hidden_field_tag key, value  %>
             <% end %>
            <% end %>
       <button class="column button button--nomargin secondary light" type="submit" >Get</button>
	  </form></div>




  <div class="row">
          <form action="https://sso.comune.milano.it:443/openam/SSOPOST/metaAlias/Servizi/IDP_CdM" method="post">
            <%= params.each do |key, value|  %>
             <% if (key == 'SAMLRequest') %>
                <%= hidden_field_tag key, value  %>
             <% end %>
            <% end %>
       <button class="column button button--nomargin secondary light" type="submit" >Post 2</button>
   </form></div>
</div>
